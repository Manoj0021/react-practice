react element returns an object the method of render renders the object as an html tags
order of files matter a lot in react
you can apply react only for the part of the app as we select root in react it is only applied to it.